name: Deploy API Parking

on:
  push:
    branches: [ "main" ]

jobs:
  call-pipeline:
    uses: alejocarobolante/pipelines-cicd/.github/workflows/prod.yml@main
    secrets: inherit
    with:
      IMAGE_NAME: go-api-sistema-parking
